apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: cursedbot
spec:
  serviceAccountName: trigger-account
  triggers:
  - name: cursedbot-build
    template:
      ref: cursedbot-build